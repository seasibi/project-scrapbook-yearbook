Drop the master coder's photo here as:

    master-coder.jpg   (or .png / .webp / .jpeg)

It fills the taped polaroid in the "message from the master coder" section.
A portrait (3:4) crop looks best. Until a file is added, a "drop a photo
here" placeholder keeps the frame's shape.
